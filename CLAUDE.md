# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(claude.ai/code)에게 제공하는 가이드입니다.

## 프로젝트 현황

TicketRush는 Spring Boot 애플리케이션입니다 (Spring Boot 4.1.0, Java 21, Gradle). 저장소는 classq(`all/classq`)와 동일하게 `ticketrush-backend/`(Gradle 프로젝트 전체)와 `ticketrush-frontend/`(현재 빈 폴더, 기술스택은 React(Vite)로 확정 — 데모용 골든 패스 화면만, 3주차와 병행 예정) 폴더로 나뉘어 있고, 루트에는 `docker-compose.yml`/`CLAUDE.md`/`.claude/`만 있습니다. **1주차 구현 항목이 전부 완료된 상태**입니다 — `account`/`event`/`section`/`seat` 엔티티, 회원가입·로그인·재발급·로그아웃, JWT 발급·검증 필터, Refresh Token(httpOnly Cookie + Redis `refresh_token:{accountId}`), 공통 예외 처리, ADMIN 계정 자동 생성, ORGANIZER 승인 API, 이벤트 등록/전체교체/삭제/조회와 Redis `seat_status:{eventId}` 초기화, 대기열 진입/순번 폴링 조회(Redis Sorted Set `queue:{eventId}`)와 입장 토큰 Scheduler(Redis String `entry_token:{eventId}:{accountId}`, 오픈 전 이벤트는 건너뜀)가 동작합니다. 포트원(V2) 웹훅 수신도 임시 스모크테스트로 연결 확인까지 마쳤습니다(검증용 코드는 확인 후 제거함) — PG는 토스페이먼츠(카드)+카카오페이(간편결제) 2채널로 확정(decisions.md 5번). **2주차**: 좌석 상태 모델(단일 좌석 홀드/해제), 홀드 TTL/만료 처리, Saga 상태머신이 완료됐습니다. `seat` 도메인에 `SeatStatusRepository`·`HoldRepository`·`ActiveReservationRepository`·`HoldScheduleRepository`·`SeatService`·`HoldExpiryScheduler`가 있고, `reservation` 도메인에 `Reservation`/`ReservationSeat`/`ReservationStatus` 엔티티와 `ReservationService`(`requestPayment`/`confirmPayment`/`markPaymentFailed`/`releaseAfterFailure`)가 동작합니다. 홀드 만료 처리는 설계 문서(redis-design.md)의 원안이던 Redis Keyspace Notification 대신 "만료 시각순 정렬 집합(`hold_schedule`) + 주기적 스케줄러" 방식으로 구현 단계에서 재설계했습니다(pub/sub 유실 위험과 만료 시점엔 키 값을 읽을 수 없는 문제 때문, 사용자 확인 완료) — `docker-compose.yml`의 Redis도 문서 원안대로 AOF/RDB를 껐습니다. Saga 상태머신은 실제 PG 호출 없이 상태 전이 로직만 구현했고(사용자 확인 완료, 3주차 결제 연동에서 실제 웹훅과 이어붙일 예정), 확정/실패 전이는 이 프로젝트 첫 자동 테스트(`ReservationServiceTest`, JUnit)로 검증합니다. **분산락 벤치마크(2주차 마지막 항목)도 구현까지 완료**했습니다 — 그룹 좌석 홀드(좌석 2개 동시 선택)를 `GroupHoldLockStrategy` 인터페이스로 추상화하고, Redisson RLock(`RedissonGroupHoldLockStrategy`)과 DB 비관적 락(`DbPessimisticLockGroupHoldLockStrategy`, `SELECT ... FOR UPDATE`) 두 구현을 모두 만들어 `group-hold.lock-strategy` 프로퍼티(기본값 `redis`)로 전환할 수 있게 했습니다. `SeatService`/`ReservationService`의 홀드·Saga 로직은 좌석 1개 전제에서 좌석 목록(1~2개) 전제로 확장했습니다. **분산락 최종 채택 = Redisson RLock (2026-09-03 벤치마크로 확정, `test-results.md` 3번·`decisions.md` 2번)** — 300명 완전 동시 / 좌석 4개로 비교한 결과 성능은 동등(처리량·Global P99), 유일한 차이는 DB 락이 `REQUIRES_NEW` 트랜잭션마다 HikariCP 커넥션을 물어 pending 147까지 쌓인 것(Redisson 0). Redis는 이미 코어라 "동등하면 DB 락(추가 인프라 불필요)" tie-breaker의 근거가 안 맞고, DB 락은 부하 커지면 커넥션 고갈로 먼저 무너질 자원이라 Redisson 유지. 선행으로 DB 락에 3초 timeout + `GROUP_HOLD_LOCK_TIMEOUT` 매핑을 넣어(커밋 `8570d91`) 실패 방식을 맞춘 뒤 측정했습니다. `aws-spec.md` B-2도 RDS `db.m6i.large` 확정. **이것으로 2주차의 마지막 열린 결정까지 닫혔습니다.** **데모용 프론트엔드(React/Vite/TypeScript)도 원래 3주차 계획을 오늘(2026-08-23)로 앞당겨 골든 패스(회원가입/로그인 → 이벤트 목록/상세 → 대기열 → 좌석 홀드 → 결제 요청)를 완료**했습니다(결제 확정은 3주차 결제 연동 이후). **3주차 인프라를 최종 확정(2026-08-27)**했습니다 — EKS/ElastiCache/MSK/CloudWatch는 도입하지 않고 AWS EC2 + Docker Compose + RDS(MySQL)만 씁니다(decisions.md 10번, "관리형 서비스를 써봤다"보다 분산락 벤치마크처럼 근거 있는 선택이 신입 채용 포트폴리오에 더 강하다는 판단). 3주차 일정 순서도 decisions.md 13번 원안대로 카오스/부하테스트가 AWS 배포보다 먼저인 것으로 정정했습니다. **3주차 첫 항목인 Kafka exactly-once + 결제 연동도 완료(2026-08-27)**했습니다 — `outbox_events` 엔티티, `ReservationService.markPaymentFailed`의 outbox INSERT, Debezium Outbox Event Router SMT 커넥터(등록 스크립트 `scripts/register-outbox-connector.ps1`), Kafka Consumer(`PaymentFailedConsumer`)가 `PAYMENT_FAILED` 이벤트를 받아 `releaseAfterFailure`(좌석 반납)를 트리거합니다(decisions.md 5번 Choreography 구현, `PAYMENT_CONFIRMED` 쪽은 정산/알림 기능 자체가 보류 중이라 아직 없음). `POST /api/v1/payments/webhook`(포트원 V2 웹훅, Standard Webhooks 서명 검증 — 구현 당시엔 시크릿을 못 찾아 가정한 스펙으로만 검증했으나, 같은 날 사용자가 콘솔에서 실제 시크릿을 찾아 `.env`에 반영함), `GET /reservations/me`·`GET /reservations/{id}`·`POST /reservations/{id}/cancel`도 함께 구현했습니다. **같은 날 프론트엔드에 "내 예약" 화면(목록/취소)을 추가하고 결제 요청 결과를 폴링해서 보여주도록 바꿨습니다.** Nginx(로컬 Docker Compose, 대기열 진입 API만 Rate Limiting, decisions.md 4번)와 **Prometheus + Grafana(로컬, 카오스/부하테스트 관찰용 — API 응답시간/에러율·Kafka Consumer lag·DB 커넥션 풀을 `/actuator/prometheus` 하나로 전부 확인, classq 참고)**는 2026-08-28에 전체 스택 기동 + `gradlew clean build` + 런타임 지표/rate limit 재검증까지 마치고 커밋했습니다(`nginx/`, `prometheus/`, `grafana/`, `docker-compose.yml`; Grafana는 `grafana_data` 볼륨 포함). 프론트의 실제 PG SDK 연동(결제창 호출)은 아직 없어 데모는 여전히 "결제 요청 완료 → 결과 폴링" 화면에서 끝납니다. **3주차 남은 항목(카오스 테스트 → 부하테스트+분산락 최종 채택 → AWS 배포)은 4주차로 넘기지 않고 3주차 안(~08-30)에 끝내는 것을 목표로 합니다(2026-08-27 확정)** — AWS 계정 가입은 완료된 상태입니다. **2026-08-28에 카오스 테스트 준비(Phase 1)를 끝냈습니다**(스모크 검증까지): ① 카오스 중 부하 발생도 Gatling으로 통일(부하테스트에서 쓸 시나리오를 먼저 만들어 재사용, decisions.md 8번) — `io.gatling.gradle` 3.15.1.3 플러그인 + `src/gatling/java/simulation/GoldenPathSimulation.java`(가입→로그인→대기열→좌석 조회→홀드→결제 요청, 좌석 ID는 응답에서 직접 추출), 스크립트 `scripts/{seed-load-test,run-gatling,chaos-redis,chaos-kafka}.ps1`. ② 분산락 채택 기준에 그룹 홀드 P99와 락 실패 응답 형태(즉시/대기 후)를 추가(멘토 피드백, decisions.md 2번) — 벤치마크 전에 DB 비관적 락에 lock timeout을 걸어 실패 방식을 Redisson과 맞추는 선행 수정 필요. `application.properties`에 P99 히스토그램 설정 추가. ③ Grafana 대시보드 provisioning(`grafana/dashboards/ticketrush.json`, 4패널: 응답시간 P50/P95/P99·상태코드별 요청/에러율·Kafka lag·HikariCP). ④ AWS 인스턴스는 m계열 방향(classq는 앱 전용 c계열이지만 우리는 EC2 한 대에 Boot+Redis+Kafka+Connect+Nginx 공존이라 RAM도 병목) — 잠정 `m6i.xlarge`, RDS는 락 결과에 따라, 상세는 신규 `.claude/docs/aws-spec.md`(A·B 작성 완료 / C·D·E는 부하테스트 후). **테스트 실행 전 계획도 문서화했습니다(2026-08-28)** — `.claude/docs/test-plan.md`(목표 수치 기준선·카오스 2 시나리오·분산락 벤치마크·한계 테스트, 각 값에 근거)와 `.claude/docs/test-results.md`(실측값 단일 출처, 아직 전부 대기). 목표 기준선: 오버셀 0 / 동시 300명 / P95 홀드~결제 < 2s / P99 그룹홀드 < 3s / 에러율 < 1% / Redis 복구 < 30s / Kafka lag 0 도달 < 60s. **다음은 Phase 2 — test-plan.md 2번(카오스) → 3번(분산락 벤치마크, 선행: DB 락 timeout 수정) → 4번(한계 테스트) 실행이며, 사용자가 시작 신호를 줄 때 진행합니다.** 순서는 `decisions.md` 13번을 따릅니다. `.claude/docs/` 아래 설계 문서(`decisions.md`, `architecture.md`, `db-schema.md`, `redis-design.md`, `api-design.md`, `aws-spec.md`)는 1차 작성이 완료된 상태이고, 테스트 문서(`test-plan.md`, `test-results.md`)가 2026-08-28에 추가됐습니다. 이후 구현 단계에서 드러난 세부 사항(에러 코드 추가, 상한값 조정 등)도 계속 반영되고 있습니다(`decisions.md` 13번 구현 순서 참고). 로컬 개발/테스트용 `docker-compose.yml`(MySQL, Redis, Kafka(KRaft), Kafka Connect+Debezium)과 `application.properties`의 DB/Redis/Kafka 연결 설정은 작성 및 실제 기동 확인까지 완료했습니다(`docker compose up -d` → `gradlew.bat bootRun` 정상 연결 검증됨). **Phase 2 실행 착수 전인 2026-09-01에 테스트 설계를 세 가지 보완**했습니다(사용자 문제 제기, 상세는 progress.md 2026-09-01 항목) — ① 부하 시나리오에서 회원가입/로그인을 빼고 `seed-load-test.ps1`가 미리 로그인까지 끝낸 BUYER 계정 풀(`buyers.csv`)을 쓰도록 변경, ② 대기열 진입 투입 방식을 카오스용(버스트+트리클)과 분산락 벤치마크용(완전 동시)으로 분리(`inject.mode`), ③ AWS 배포 전 로컬에서 같은 리소스 제한(EC2/RDS 스펙)으로 먼저 재현하는 `docker-compose.rehearsal.yml` + 앱 `Dockerfile` 신설(빌드~기동~헬스체크 검증 완료). **카오스 테스트 A-1(Redis 다운) 준비 중 도구 버그 2건(이 PC PowerShell에 docker 미탐지, test-plan.md 오버셀 SQL의 존재하지 않는 컬럼/값 참조)과, 설계 문서에만 있고 코드엔 없던 "Redis 데이터 유실 복구(rebuild)" 로직 자체의 부재를 발견해 전부 고쳤습니다**(`SeatStatusRebuildService` 신규, portfolio.md 소재 6, decisions.md 1번·redis-design.md 6번). 추가로 `spring.data.redis.timeout=2000`(미설정 시 Lettuce 기본 60초라 장애 중 요청이 복구 후에도 60초 매달림), `chaos-*.ps1`의 stop/start UTC 타임라인 로그, Gatling chaos 모드 꼬리 부하 옵션(`-TailSeconds`), Grafana 대시보드 한글화를 반영했습니다. **카오스 A-1 실행 완료(2026-09-03, `test-results.md` 1번, 스크린샷 `.claude/screenshots/tests/a1-redis-down/`)** — 클린 스택에서 폭주 중 Redis 61초 완전 다운→복구: **오버셀 0 / seat_status 재구성 완료 ~4초 / rebuild 1회(락 경합 요청은 503) / 최대 응답시간 2,035ms(타임아웃 2초 cap 확인)**. KO의 79%는 대기열 유실(`QUEUE_ENTRY_NOT_FOUND`, decisions.md 1번 "알려진 한계"), 진짜 서버 버그성 실패는 0. **카오스 A-2(Kafka 브로커 다운)도 실행 완료(2026-09-03, `test-results.md` 2번, 스크린샷 `.claude/screenshots/tests/a2-kafka-down/`)** — 신규 `scripts/fail-payments.ps1`(부하 중 일부 예약에 서명된 `Transaction.Failed` 웹훅을 쏴 outbox→Kafka 경로를 태움)와 함께, Kafka를 91초 완전 다운→복구: **장애 중 결제 요청·웹훅 5xx 0건 / 이벤트 유실 0(outbox 239 = SEAT_RELEASED 239) / Consumer lag ~15초 만에 0(커넥터 수동 재시작 후) / 장애 중 API P99 ~60ms로 평상시와 동일**. Outbox 패턴이 Kafka를 critical path에서 뺀 것이 실측 확인됨. 발견: Kafka 다운 시 Debezium 커넥터가 `UNASSIGNED`로 떨어져 자동 복구가 안 돼 수동 재시작 필요. ③분산락 벤치마크도 완료(위 "분산락 최종 채택" 참고). **④한계 테스트 1차도 실행 완료(2026-09-04, `test-results.md` 4번)** — 리허설 스택을 이 PC RAM 16 GiB에 맞춰 축소(원안 24 GiB 상당 → 합 ~7 GiB, `docker-compose.rehearsal.yml` 재조정)하고 `~/.wslconfig`(`memory=8GB` + `autoMemoryReclaim`)를 신설. 계단식 주입은 이 시스템(계정당 이벤트 1건 제한)에 안 맞아 버리고 **버스트(1,500명 완전 동시) 모델로 전환**, 설정·경로 바꿔가며 3회 실행 — **전부 오버셀 0 / 5xx 0**(정합성 완벽). **동시 인원 한계 숫자는 로컬 localhost로는 못 냈다** — "Connection refused"가 앱이 아니라 **Docker Desktop 유저랜드 포트 프록시**(Windows↔WSL2)에서 나는 것으로 판명(`:8080`/`:8081` 무관 ~500~760/1500 거부, `accept-count` 100→2000 무효). AWS엔 이 계층이 없으므로 진짜 숫자는 AWS 재측정 또는 Gatling-in-container(`test-plan.md` 4-4)로 확정. 병목축은 **app CPU(2 vCPU cap) → DB CPU**로 확정(`aws-spec.md` C — HikariCP는 10→20 올려도 pending 그대로라 풀 크기가 레버가 아님, 기본값 10 유지). 신규 파일: `docker-compose.capacity.yml`(nginx rate-limit 제거판), `CapacitySimulation.java`, `nginx/nginx.capacity.conf`, `scripts/{run-capacity.ps1,seed-buyers-parallel.mjs,login-buyers.mjs}`, `src/gatling/resources/logback.xml`. **이 세션 변경분은 커밋 완료(`61b8ab5`/`bbbd988`/`e7d428b`)했고, 이어서 **Gatling-in-container 재측정도 완료**했습니다 — `docker-compose.capacity.yml`에 `gatling` 서비스를 추가해 Gatling도 컨테이너 네트워크 안에서 `nginx:80`을 직접 쳐 Docker Desktop 포트 프록시를 우회했습니다. 첫 시도(이벤트만 새로 만들고 DB/Redis는 리셋 없이 1,500→2,000→1,750→800명 순으로 4회 실행)는 프록시 우회는 성공했지만 동시인원과 서버측 P95가 반비례하는 이상값이 나왔는데, 사용자가 "테스트마다 DB 지우면 되잖아"로 문제를 제기해 원인을 좁혔습니다 — 데이터 누적(InnoDB 버퍼풀 128MB 안에 여유 있게 들어감, 반증됨)이 아니라 **테스트 간 상태를 리셋하지 않은 것 자체**가 원인이었습니다. 매 실행 전 `reservation`/`reservation_seat`/`outbox_events` TRUNCATE + 테스트 이벤트/구역/좌석 DELETE + Redis `FLUSHALL`로 완전히 동일한 초기 상태를 만들고 N을 100→250→350→450→500으로 늘려가며 재측정한 결과, **100~450명은 완만(서버 P95 6ms→132ms)하다가 450→500 사이에서 절벽**(132ms→8,990ms, 68배 폭증)이 나타났습니다 — 이 축소 스펙(app 2 vCPU)의 실제 한계는 ~460~480명 부근으로 추정되며, 오버셀은 전 구간 0으로 정합성은 무너지지 않았습니다(`test-results.md` 4-4). all/classq의 `load-test.md`(HikariCP 대기·BCrypt cost 등 원인 특정 → 조치 → Before/After 재측정으로 P95 최대 -99.95% 개선한 선례)를 참고해, **"한계 발견"에서 멈추지 않고 원인 진단(HikariCP/Tomcat/GC 중 실질 상한 특정) → 튜닝 → Before/After 재측정까지 하기로** 사용자와 정리했습니다 — 기존 "풀 크기가 레버가 아니다"(호스트 기반, 1,500명대 측정) 결론은 절벽 지점 자체를 못 본 상태에서 나온 것이라 재검증이 필요합니다. **③ 원인 진단 + ④ 튜닝/Before-After도 완료했습니다(2026-09-05, `test-results.md` 4-5)** — 4-4의 정확한 절벽 재현은 실패했지만(리셋 절차 미기록으로 3번의 방법론 오류를 거침, 상세는 4-5) 오류 없는 새 기준값을 확보해 공식 채택했습니다. 원인 진단 결과 **HikariCP 커넥션 풀(최대 10개)이 가장 먼저 병목**입니다(N=100에서 이미 Tomcat 스레드는 60% 유휴인데 DB 커넥션 대기 67건, N=250부터 Tomcat도 포화되지만 이건 결과일 뿐 원인이 아님, GC는 5% 미만이라 무관) — 구체적으로는 절대 안 바뀌는 좌석 배치도(최대 15,000행)를 `GET /seats`가 매번 DB에서 다시 읽고 있었습니다. **사용자가 "근본적으로 코드를 고쳐야 하지 않냐"고 지적**해 `SeatCatalogRepository`(신규)로 좌석 배치도를 이벤트 등록 시 한 번만 DB에서 읽어 Redis에 캐싱하고 이후 조회는 캐시만 쓰도록 수정했습니다(캐시 미스 시 DB 폴백 + 재채움, `SeatStatusRebuildService`와 같은 자가복구 패턴). Before/After 재측정 결과 **N=450 이하 구간 seat-hold P95 32~46% 개선**됐으나 **N=500은 여전히 9초대(-13%뿐)**이고 HikariCP pending도 일부 구간에서 오히려 늘었습니다(좌석 조회가 빨라지며 사용자들이 DB를 쓰는 다음 단계(홀드/결제)에 더 몰림) — "부분 개선, DB 커넥션 풀 자체가 여전히 근본 상한"이라는 게 정직한 결론입니다. **같은 세션에 이어서 트랜잭션 범위 축소도 시도했습니다** — `hold()`/`requestPayment()`가 메서드 전체를 `@Transactional`로 감싸 DB와 무관한 Redis 호출(입장 토큰 검증, 그룹 홀드 락 대기 최대 3초)까지 DB 커넥션을 붙잡던 걸 발견해 실제 DB 필요 구간만 남기고 나머지를 밖으로 뺐습니다(`requestPayment`는 신규 `TransactionTemplate`으로 쓰기 구간만 좁힘). 리팩터링 직후 `gradlew test`에서 17개 중 12개가 `LazyInitializationException`으로 실패하는 실제 회귀를 잡아내 `SeatRepository`에 `JOIN FETCH` 쿼리를 추가해 해결(17개 재통과) — **완전히 위험 없다던 초기 판단이 틀렸음이 실측으로 확인된 사례**입니다. 다만 Before/After 재측정에서는 개선 대신 **새 병목(Redis, 이 리허설의 0.5 vCPU 제한)이 노출**돼 N=350~500에서 실패(KO)가 새로 발생했고, Redis를 1.0 vCPU로 올려도 개선 안 됨(싱글 스레드 구조 한계)을 확인한 뒤 **로컬 튜닝은 중단**했습니다(시간 제약, 사용자 확인 완료). 트랜잭션 축소 코드 자체는 구조적으로 옳아 그대로 채택하지만, 효과 측정은 AWS 재측정으로 미룹니다. **대기열 이탈률 섞은 부하테스트 시나리오(decisions.md 4번, 2026-08-27부터 미실행 상태로 남아있던 계획 항목)도 AWS 배포 직전에 마저 실측했습니다(2026-09-05, `test-results.md` 5-1)** — `GoldenPathSimulation`에 `dropout.ratio`(진입만 하고 폴링 안 하는 이탈자 흉내) 추가, 실사용자 60명 고정·이탈률 0/30/50%로 전체 진입만 늘려보니 실사용자 체감 대기(평균 폴링 횟수)가 최대 83% 늘어남을 확인(오버셀 0, 정합성 무관한 순수 UX 지표). **이걸로 AWS 배포 전 로컬에서 하기로 했던 테스트가 전부 끝났습니다. 다음은 ⑤ AWS 배포 → ⑥ AWS 재측정입니다**(HikariCP 풀 크기 추가 튜닝도 그때 같이 재검토). `.claude/progress.md`는 문서화/구현 진행 상황과 추후 결정이 필요한 사항, 4주 일정을 추적한다(재기동 체크리스트는 `test-plan.md` 4-5). 프로젝트가 채워짐에 따라 각 문서의 최신 내용을 확인하세요.

## 명령어

모든 명령어는 `ticketrush-backend/` 디렉토리에서 실행합니다. Gradle 래퍼(Windows에서는 `gradlew.bat`, POSIX 셸에서는 `./gradlew`)를 사용하세요 — 시스템에 설치된 Gradle에 의존하지 마세요.

- 빌드: `gradlew.bat build`
- 앱 실행: `gradlew.bat bootRun`
- 전체 테스트 실행: `gradlew.bat test`
- 특정 테스트 클래스 실행: `gradlew.bat test --tests "com.ticketrush.ticketrush.TicketRushApplicationTests"`
- 특정 테스트 메서드 실행: `gradlew.bat test --tests "com.ticketrush.ticketrush.TicketRushApplicationTests.methodName"`

## 아키텍처

- `ticketrush-backend/`: 기본 패키지 `com.ticketrush.ticketrush`, 루트 클래스는 `TicketRushApplication` (`ticketrush-backend/src/main/java/com/ticketrush/ticketrush/TicketRushApplication.java`).
- `ticketrush-frontend/`: **React(Vite) + TypeScript, 데모용 골든 패스 화면 완료(2026-08-23, 원래 3주차 계획을 오늘로 앞당김, 사용자 확인 완료)**. 회원가입/로그인(`SignupPage`/`LoginPage`) → 이벤트 목록/상세(`EventListPage`/`EventDetailPage`) → 대기열 진입·순번 폴링(`QueuePage`) → 좌석 선택·홀드(`SeatHoldPage`, 지정석 그리드/스탠딩 수량 모두 지원) → 결제 요청까지 브라우저에서 실제로 끝까지 확인했다. **결제 요청 이후 결과 확인은 2026-08-27에 추가됨** — `SeatHoldPage`가 `GET /reservations/{id}`를 폴링해 `PAYMENT_CONFIRMED`/`PAYMENT_FAILED`/`SEAT_RELEASED` 결과를 보여주고, 신규 `ReservationsPage`("내 예약")에서 목록 조회와 `PAYMENT_CONFIRMED` 예약 취소가 가능하다. 다만 **실제 결제창(포트원 SDK) 연동은 아직 없어서**, 웹훅이 오려면 지금은 누군가 직접 웹훅을 흉내 내 호출해야 한다 — 브라우저 골든 패스만으로는 결제 요청 이후가 계속 "처리 중"으로 멈춰 있다. 인증 상태는 `AuthContext`가 담당(Access Token은 메모리에만 두고, 새로고침 시 `/auth/refresh`로 세션을 조용히 복구), Access Token 만료 시 API 클라이언트(`api/client.ts`)가 자동으로 재발급 후 원요청을 한 번 재시도한다. 관리자 화면과 주최자(ORGANIZER)의 이벤트 등록 화면은 범위 밖(progress.md 참고, 이벤트는 API로 직접 등록).
- **패키지 구조는 도메인별로 나눈다**(사용자 확인 완료). `domain/{account,event,queue,seat,reservation}` 각각을 `controller/dto/entity/repository/service` 레이어로 구성하고, 도메인과 무관한 공통 코드는 `global/{config,entity,exception,jwt}`에 둔다. Kafka producer/consumer나 scheduler처럼 특정 도메인에만 필요한 것은 해당 도메인 하위에 폴더를 추가한다. 계정 관련 코드는 API 경로가 `/api/v1/auth/*`여도 패키지는 DB 테이블명과 맞춘 `domain/account`에 둔다(ADMIN의 계정 승인처럼 "인증"이 아닌 계정 관리 기능도 함께 들어가기 때문).
- 의존성 스택 (`ticketrush-backend/build.gradle` 기준): Spring Web MVC, Spring Data JPA, Spring Security, Bean Validation, Lombok, MySQL 커넥터(런타임), Spring Boot DevTools(개발용).
- 영속성 대상은 `com.mysql:mysql-connector-j`를 통한 MySQL. `ticketrush-backend/src/main/resources/application.properties`에 `spring.datasource.*`/`spring.data.redis.*`/`spring.kafka.bootstrap-servers`가 `${ENV_VAR:localhost 기반 기본값}` 패턴으로 설정되어 있어, 로컬에서는 별도 환경변수 없이 `docker-compose.yml`만 띄우면 바로 연결된다(나중에 앱을 컨테이너화해도 환경변수 주입만으로 재사용 가능, classq 패턴 참고).
- **비밀값은 `ticketrush-backend/.env`에 두고** `spring.config.import=optional:file:.env[.properties]`로 읽는다(사용자 확인 완료). `.env`는 gitignore 대상이라 저장소에 없으므로, 새 환경에서는 직접 만들어야 한다 — 필요한 키는 `JWT_SECRET`(HS256 이상이라 32바이트 이상), `JWT_ACCESS_EXPIRATION`(ms), `JWT_REFRESH_EXPIRATION`(ms), `REFRESH_COOKIE_SECURE`(로컬은 `false` — https가 아니면 Secure 쿠키가 오가지 않는다), `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `PORTONE_WEBHOOK_SECRET`(`whsec_` 접두사 + base64 — 없으면 웹훅을 전부 거절한다. 카오스 A-2의 `scripts/fail-payments.ps1`이 이 값으로 `Transaction.Failed` 웹훅에 서명한다).
- **테이블은 `spring.jpa.hibernate.ddl-auto=update`로 엔티티에서 자동 생성**한다. db-schema.md의 생성 컬럼(`reservation_seat.active_seat_id`)과 CHECK 제약은 이 방식으로 표현되지 않는데, `reservation_seat`를 구현한 시점(2주차 Saga 상태머신)에 **Flyway는 도입하지 않고 애플리케이션 레벨 검증으로 대체하기로 확정**했다(사용자 확인 완료) — `ReservationSeatRepository.existsBySeatIdAndStatusIn`가 2차 방어선 역할을 한다(db-schema.md 6번 참고). 그래서 `reservation_seat` 테이블에는 `active_seat_id` 컬럼이 실제로 없다.
- **좌석 대량 생성만 JPA가 아니라 `JdbcTemplate` batch를 쓴다**(`SeatBulkInsertRepository`). `seat.id`가 AUTO_INCREMENT라 JPA(IDENTITY 전략)에서는 `hibernate.jdbc.batch_size`를 켜도 배치가 적용되지 않고, 구역 하나가 수천 좌석이 될 수 있기 때문이다. JDBC URL의 `rewriteBatchedStatements=true`와 짝이므로 둘 중 하나만 빠져도 배치가 무력화된다 — 좌석 생성 성능을 건드릴 때는 MySQL `Com_insert` 상태값으로 실제 INSERT 문 개수를 확인할 것.
- **Spring Boot 4는 Jackson 3을 쓴다** — `ObjectMapper`의 패키지가 `com.fasterxml.jackson.databind`가 아니라 `tools.jackson.databind`다. 같은 이유로 JJWT의 JSON 처리기도 Jackson 2를 요구하는 `jjwt-jackson` 대신 `jjwt-gson`을 쓴다.
- **Spring Boot 4에서 Kafka를 쓰려면 `spring-kafka`만으로는 안 되고 `org.springframework.boot:spring-boot-starter-kafka`가 필요하다** — `KafkaAutoConfiguration`이 `spring-boot-autoconfigure`에서 빠져 `org.springframework.boot.kafka.autoconfigure` 패키지의 이 스타터로 옮겨갔다(Boot 3까지의 "라이브러리만 추가하면 자동 설정된다" 관행과 다름). `spring-kafka`만 넣으면 에러 없이 앱이 기동되지만 `@KafkaListener`가 전혀 등록되지 않는 조용한 실패로 나타난다(실제로 겪음, 2026-08-27, portfolio.md 소재 5).
- 로컬 개발 인프라는 프로젝트 루트 `docker-compose.yml`(MySQL/Redis/Kafka(KRaft, Zookeeper 없음)/Kafka Connect+Debezium/Nginx/Prometheus/Grafana)로 제공된다. `docker compose up -d`로 기동하며 백엔드는 호스트에서 `gradlew bootRun`. Debezium outbox 커넥터는 `scripts/register-outbox-connector.ps1`로 등록한다(컨테이너를 새로 띄울 때마다 재등록 필요 — Kafka에 볼륨이 없어 재기동 시 등록 상태가 초기화됨).
- **앱 `Dockerfile`(2026-09-01 신규)은 두 곳에서만 쓴다** — (1) `docker-compose.rehearsal.yml`(AWS 스펙 리소스 제한으로 로컬 리허설, 아래 참고), (2) 실제 AWS 배포(3주차 마지막 항목, 아직 착수 전). **평소 개발은 여전히 `gradlew bootRun`**(핫 리로드 등 개발 편의 때문에 그대로 유지, 사용자 확인 완료).
- **`docker-compose.rehearsal.yml`(2026-09-01 신규)은 AWS 배포 전 로컬에서 리소스 제한을 걸어 미리 재현하는 오버레이**다 — "AWS에 돈 태우고 나서야 못 버티는 걸 알면 그 상태로 계속 고쳐야 한다"는 문제의식(사용자 확인 완료). 평소 `docker-compose.yml` 단독 사용에는 전혀 영향 없다. **2026-09-04에 예산을 이 개발 PC(RAM 16 GiB)에 맞춰 축소**했다 — 원안은 EC2 `m6i.xlarge`(16 GiB) + RDS `db.m6i.large`(8 GiB)를 그대로 흉내냈으나 합 24 GiB가 16 GiB PC에 안 들어가(부하 걸면 호스트 스왑) app 2 vCPU/2400m · mysql 1.5/2200m · kafka 0.75/800m · connect 0.5/560m · redis 0.5/300m · nginx 0.5/160m(합 ~7 GiB)로 낮췄다. "노트북이 아니라 EC2 성능"이라는 취지는 병목축 판별(CPU vs RAM)까지 유효하고 절대 수치는 AWS 재측정에서 확정한다(`aws-spec.md` C). `TZ=Asia/Seoul`(앱이 openAt를 zone 없는 LocalDateTime으로 다뤄 컨테이너 UTC면 KST seed와 9h 어긋남), `JWT_ACCESS_EXPIRATION=4h`(테스트 중 토큰 만료 방지), HikariCP/Tomcat 튜닝 노브(`HIKARI_POOL`/`TOMCAT_ACCEPT` env), nginx `depends_on: [app]`도 이때 추가. **한계 테스트는 여기에 `docker-compose.capacity.yml`을 더 얹어 진행한다**(nginx를 rate-limit 없는 `nginx.capacity.conf`로 교체 — 단일 호스트 대량 연결 시 per-IP rate limit이 전부를 429로 막는 것 회피). `docker compose -f docker-compose.yml -f docker-compose.rehearsal.yml [-f docker-compose.capacity.yml] up -d --build`. 이때만 앱도 컨테이너로 뜬다. `~/.wslconfig`(`memory=8GB` + `[experimental] autoMemoryReclaim=dropcache`)도 이 PC에 신설 — 없으면 Docker VM이 빌드 캐시를 안 돌려줘 호스트 여유 메모리가 0.6 GiB로 굶는다. 재기동 체크리스트·함정 모음은 `test-plan.md` 4-5.
- 테스트 스택은 메인 스택과 동일하게 `-test` 스타터 변형(`spring-boot-starter-data-jpa-test`, `-security-test`, `-validation-test`, `-webmvc-test`)으로 구성되며, `ticketrush-backend/build.gradle`에서 `useJUnitPlatform()`으로 설정되어 있습니다.
- **프론트엔드(Vite 개발 서버, `localhost:5173`)가 백엔드(`localhost:8080`)와 다른 포트라 CORS 설정이 필요하다**(`SecurityConfig.corsConfigurationSource`, 2026-08-23 추가). Refresh Token을 httpOnly Cookie로 주고받기 때문에 `allowCredentials=true`가 필수이고, 그러면 허용 오리진에 `*`를 쓸 수 없어 `frontend.origin` 프로퍼티(기본값 `http://localhost:5173`, `.env`의 `FRONTEND_ORIGIN`으로 재정의 가능)로 명시한다.

## 협업 방식

- 사용자가 명시적으로 말하지 않은 내용을 넘겨짚어 채우지 않는다. 예: 사용자가 "A 파일은 마지막에 업데이트할 것"이라고만 말했을 때, 언급하지 않은 B 파일도 같은 방식으로 처리할 것이라고 임의로 추측하지 않는다.
- 지시가 애매하거나, 여러 해석이 가능하거나, 잘못 판단하면 이후 작업에 영향을 줄 수 있는 상황에서는 임의로 결정하지 말고 먼저 사용자에게 질문해서 확인한 뒤 진행한다.
- 작업 중 참고할 문서가 필요하면 먼저 `TicketRush/.claude/` 하위 문서를 확인하고, 부족하면 `all/classq/.claude/docs/` 또는 `all/classq/.claude/`를 참고한다. 그래도 참고할 내용이 없으면 임의로 판단하지 말고 사용자에게 질문한다.
- 문서 작업을 진행하면서 `.claude/progress.md`(진행 상황·추후 결정 사항)와 이 `CLAUDE.md`(프로젝트 현황)가 최신 상태를 벗어나면, 별도 요청 없이도 계속 업데이트한다.
- `.claude/portfolio.md`는 프로젝트 종료 후 이력서/포트폴리오에 쓸 "문제와 해결" 소재를 모으는 문서다. 작업 중 **실제로 부딪혀서 측정하거나 고친 것**이 나오면 별도 요청 없이 여기에 추가하되, 그 문서 상단의 "수록 기준"을 반드시 지킨다 — 일반적인 CRUD 구현이나 "~를 사용했습니다" 수준의 내용은 넣지 않는다. **단, 성능 수치는 개발 중 값을 그때그때 적지 않는다**(사용자 확인 완료) — 3주차 후반 부하/장애 테스트와 모니터링을 거쳐 정식으로 측정·개선한 뒤에 채운다. 그 전까지는 "문제와 접근"까지만 기록한다.
- **커밋 메시지나 PR 본문에 Claude/AI 관련 문구를 절대 넣지 않는다**(예: `Co-authored-by: Claude ...`, "🤖 Generated with Claude Code" 같은 트레일러/푸터 전부 금지, 사용자 확인 완료). GitHub의 Contributors 그래프나 PR 화면에 Claude가 드러나는 걸 원하지 않기 때문 — 실제로 `all/classq`에서 커밋 메시지에 `Co-Authored-By: Claude ...` 트레일러가 들어가 Contributors에 노출된 적이 있어 명시적으로 금지함. 커밋 author도 그 환경의 `git config` 값(사용자 본인 계정)을 그대로 쓰고 별도로 바꾸지 않는다.